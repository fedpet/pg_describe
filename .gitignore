# build artifacts
*.o
*.so
*.bc
results/
regression.diffs
regression.out
tmp_check/

# node
node_modules/
dist/
*.tsbuildinfo

# docusaurus
website/build/
website/.docusaurus/
.cache-loader/

# editor and OS noise
.vscode/
.idea/
*.swp
*~
.DS_Store

# local environment, never committed. The wildcard covers whatever the file is
# called this week -- naming it here would only advertise it, the repository
# being public.
.env*
!.env.example
